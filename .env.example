# Entorno de La Colectiva

# URL del sitio para validar CSRF (coincidir con origen)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role (solo en servidor, NO exponer en frontend)
# Configurar en Vercel como variable de entorno privada
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Gemini API Key (Google Generative AI)
GEMINI_API_KEY=your_gemini_api_key

# Timeout para respuestas de IA en milisegundos (opcional)
AI_TIMEOUT_MS=15000